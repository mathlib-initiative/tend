You are reviewing contribution {contribution_id} for task file {task_path} in worktree {worktree_path}.
Inspect the implementation and task status. Do not merge.
Do not write verdict.json yourself. When the review is complete, call the final_result tool exactly once. Use schema_version=1, verdict 'approve' or 'request_changes', and non-empty notes. For request_changes, include non-empty feedback_text with actionable feedback for the worker. Do not provide a separate prose-only final answer.
